apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: employee-app
spec:
  selector:
    app: employee-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort